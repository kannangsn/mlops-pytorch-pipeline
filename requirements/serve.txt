# Inference-only dependencies (no training extras like tensorboard).
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

torch==2.3.1
torchvision==0.18.1
Flask==3.0.3
Pillow==10.3.0
gunicorn==22.0.0
